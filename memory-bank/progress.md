# 开发进度（Progress）

> **活文档。** 记录 `implementation-plan.md` 里每一步的完成情况，用于跨会话续接。
> 新会话开始时先读本文件，就知道上次停在哪、下一步做什么。
> **每完成一步就追加一条记录，并同步 git commit（提交信息里带步骤编号）。**

## 当前状态

- **阶段**：M1 进行中（五步纯逻辑已完成，进入云端阶段）
- **下一步**：M1-06（开通云开发环境并跑通第一个云函数）
- **当前分支**：`step/M1-05-request-expiry`（M1-05 已完成待合回 main）
- **最新 tag**：`v2.0-design`

## 里程碑

| 里程碑 | 范围 | 状态 |
|---|---|---|
| M1 | 跑通主干（不含 AI）：云开发初始化、六个集合与索引、状态机+单测、需求广场/发布页/详情页、响应/选定/完成、过期定时任务、埋点最小集与分桶字段（共 19 步，见 `implementation-plan.md`） | 未开始 |
| M2 | **AI 主干与小螺**：`aiGateway`、需求单解析与降级、五个工具的 function calling 编排（解析/建单/兜底检索/落地清单/可解释匹配）、L0/L1 自主性阶梯、关键词兜底作答、AI 离线评测集（共 16 步，见 `implementation-plan.md`） | 未开始 |
| M3 | 形成闭环：三层机审、城市社区、信任分、约定单、1v1 私信、双向评价 | 未开始 |
| M4 | 可上线运营：安全体系、管理员页面四件事、订阅消息（模板+授权+发送）、预聚合、语料播种 | 未开始 |
| M5 | 商业化与作品集：L2 分发+频控、行程清单、小红书图文、应急卡、会员；有真实用户后启动 A/B；Demo B、材料整理 | 未开始 |

> 排期于 2026-08-29 重排（D-32）：AI 主干从原 M2 拆出并提前，运营向能力顺延；零用户阶段不做真实数据实验（D-31）。

## 完成记录

> 格式：`步骤编号 | 日期 | 做了什么 | 如何验证的 | commit`

- **M1-01 | 2026-08-29 | 需求单状态转移表与 node:test 测试骨架 | `npm test` 10 个用例全绿（12 条合法边逐条、69 条非法组合、四个终态无出边、未知状态被拒、转移表冻结）；另做破坏性验证：给 matched 加一条 responded 出边后测试变红，撤销后恢复全绿 | `cf358a0`**
  - 偏差记录：计划里写的 `node --test tests/` 在 Node 26 下会把目录当模块加载并报 `Cannot find module`，实际用 `node --test "tests/**/*.test.js"`，计划文档已同步修正。
  - 遗留给 M1-02：`assertTransition` 目前只判转移合法性，不判权限，所以此刻 `open → removed` 会通过（下架本身合法，但应只有管理员能做）。

- **M1-02 | 2026-08-29 | 状态机的角色权限矩阵（四角色 × 12 条边）| `npm test` 22 个用例全绿（新增 12）；破坏性验证：给 `responded → matched` 加 responder 后三条断言变红，撤销后恢复；手工核对三种错误码的报错文案 | 待填**
  - 比 PRD 更严的三处细化（已与人确认）：① 未选定阶段（open / responded）只有需求方能取消，responder 与该单无绑定关系；② admin 只能下架，不能代做发布/选定/完成/取消，理由同 D-14 的责任归属；③ `done → rated` 已登记 owner+responder 权限但 M1 不调用（评价属 M3）。
  - 错误码分工：`ILLEGAL_TRANSITION`（转移本身不成立）与 `TRANSITION_FORBIDDEN`（转移合法但越权）必须分开，前端提示文案不同。

- **M1-03 | 2026-08-29 | 端侧枚举与字段校验，并锁定端云枚举一致 | `npm test` 30 个用例全绿（新增 8）；`schema.js` 用 8 组样例自检（3 正例 / 3 反例 / 1 提示例 / null 边界）；破坏性验证：单改端侧一个品类值后三条 parity 断言变红；开发者工具 Console 里跑通三组调用 | 待填**
  - 品类中文展示名放在云侧并同步端侧、纳入一致性断言，因为 M2 的 `parseRequest` prompt 要把中文品类名喂给模型，两处各写一套迟早不一致。
  - 值命名避坑：品类"借物易物"用 `barter`，报酬类型"以物换物"用 `goods`，避免同值不同义。
  - `schema.js` 不含任何 `wx.*`，可在 node 直接跑；端侧纯逻辑模块一律照此办理。

- **M1-04 | 2026-08-29 | A/B 分桶与埋点事件字典 | `npm test` 44 个用例全绿（新增 14）；实测 1000 个 openid 四分位分布 248/227/275/250，5000 个 openid 命中全部 100 桶无空桶；同一 openid 在两个实验 key 下落 11 与 40 号桶；破坏性验证：给桶号掺入 Math.random 后两条断言变红 | 待填**
  - 事件字典用 `active` / `planned` 两态：`track`（M1-13）只接受 active，字典外与 planned 一律拒绝，防止事件名野生增长。M2 上报 AI 事件时改的是状态字段，不是往代码里插字符串。
  - `BUCKET_COUNT = 100` 与哈希算法都不可再改，改了历史桶号即失去可比性（D-21 想避免的正是这类损失）。
  - 事件字典的 6 条结构性断言（字段齐全/命名规范/无重名/六类覆盖/字典外被拒/必填缺失被指出）与分桶用例放在同一测试文件，属零成本不变量检查，未单独扩测试范围。

- **M1-05 | 2026-08-29 | 需求单过期判定与在架上限 | `npm test` 62 个用例全绿（新增 18）；实测夏令时伦敦「今天内」= `22:59:59.999Z`、冬令时 = `23:59:59.999Z`、同刻发布的纽约单算到当地 6/30 结束；破坏性验证：把 24h 宽限期改成 12h 后两条断言变红 | 待填**
  - 边界口径：`isExpired` 用 `now > expireAt`，**到点那一毫秒仍然有效**。
  - 「今天内」在当地深夜发布时有效期会很短（伦敦 23:30 发单只剩半小时）。这是 PRD 4.1 的字面语义，未加"至少一小时"的补偿，如需改是产品决策。
  - `checkActiveLimit` 的显式 `limit` 优先级高于会员档，实际取值将来从 `configs` 下发；非法在架数量一律报错而非放行。

## 已知问题与欠债

> 实现时发现但当下没解决的问题记在这里，不要让它只存在于对话里。

- **开发者工具 proxy 报错（2026-08-29 已解决，留档备查）**：微信开发者工具 Stable 2.01.2510280 曾在启动时弹 `代理设置失败：init proxy error TypeError: proxyCache.getProxyCachePath is not a function`，连带后果是下载不了其他版本调试基础库、Console 报 `timeout at WAServiceMainContext`。已确认现在启动**不再报错**。根因判断留档：系统存在 PAC 自动代理（`AutoConfigURL` 指向 `bpac.weiyun.baidu.com`，由公司零信任客户端设置，本地代理端口 127.0.0.1:17891），该版本工具的 proxy 模块解析系统代理时崩溃。**若复发**：① 工具内 `设置 → 代理设置 → 不使用任何代理`，完全退出重启 ② 换开发者工具版本 ③ **不要去改系统 PAC 配置**——那是公司内网接入配置，清掉会影响 Comate/iCode/ku 等内网访问。
- **基础库版本保持 3.15.0（不改，已定）**：`project.config.json` 里实际是 `"libVersion": "3.15.0"`（正式版，非灰度）。因只发体验版、用户是被邀请的少数人，覆盖率低不构成风险，**不做版本调整**。此前记录里的"trial 灰度版"与"暂留 3.15.2"两种说法均与实际不符，已作废。
- **待看**：调试器面板的警告未逐条排查（含 HarmonyOS 兼容性提示），M1-01 开工前后过一遍，确认没有 V1.0 遗留的真实问题。不阻塞纯逻辑步骤。

## 待核实事项的核实进度

对应 `design-document.md` 附录 C 与 `tech-stack.md` 第 15 节。**动手前必须先核实的项要在这里打勾。**

- [ ] 微信小程序个人主体的类目限制与 UGC 禁区（当期规则）
- [ ] 微信小程序体验版的体验成员数量上限
- [ ] 一次性订阅消息的规则与可用模板类目
- [x] **微信云开发当期免费额度** —— 已查官方计费说明（2026-08 查询）：自 2025-02-19 起每个无云环境的小程序账号可创建 1 个免费云环境，**在小程序未发布上线阶段无需续费**；小程序正式上线后到期时间变更为上线后第 15 天，之后需转付费。活动截止 2026-12-31。免费环境配额与基础套餐一致：调用 20 万次/月、容量 2GB、云函数外网出流量 2GB、云函数资源 10 万 GBs、CDN 5GB、QPS 500；不支持加购资源包与按量付费。基础套餐 ¥19.9/月（原价 ¥39）。
      **结论**：本项目按 D-16 只发体验版、不正式上线，因此免费环境足以覆盖全程，云开发实际成本为 0。
      **注意**：开发者工具导入对话框的文案写的是"首次开通可免费体验一月"，与官方文档"未上线阶段无需续费"口径不一致；腾讯云 CloudBase 侧还有"3000 资源点/月"与"40000 资源点/月体验 6 个月"等第三套口径。**以云开发控制台实际显示的到期时间为准**，开通后第一件事就是去控制台确认。
- [ ] `security.msgSecCheck` 对非中国大陆用户内容的可用性
- [ ] 云数据库实时推送（watch）并发连接数限制
- [ ] 所选模型 API 的价格、并发限制、是否支持 JSON Schema 约束输出
- [ ] 云函数出境访问模型 API 是否需额外配置
- [ ] iOS 端虚拟权益规则与站外购买引导的可行性边界
- [ ] "喊呗" / "HeyBay" 的小程序名占用与商标近似查询
- [ ] **手机号快速验证接口对个人主体小程序是否开放**（当前按"不可用"处理：L1 身份改为纯微信授权登录，不采集手机号，见 D-26。若核实为可用，也不改回——去掉手机号本身符合数据最小化）
- [ ] **微信云函数运行时是否带完整 ICU（`Intl` 的 `timeZone` 是否真的生效）** —— M1-05 的「今天内」过期判定依赖它。风险点在于缺 ICU 时 `Intl` **不报错**、只是静默回落到运行时本地时区，结果会悄悄算错。代码里已用「2026-07-01 伦敦 = UTC+1」自检并在不支持时抛 `MISSING_TIMEZONE`，退路是改传 `utcOffsetMinutes`。**M1-06 部署第一个云函数时顺手核实**（`ping` 的返回值里带了 `icu` 字段）。
