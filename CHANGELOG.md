# CHANGELOG

本项目版本由 git tag 管理，目录与仓库名不带版本号。

## [Unreleased]

### M1 主干（不含 AI）

- 云开发初始化、六个集合与 10 条索引，数据库权限收成「所有用户不可读写」——端侧读写都被拒，一切数据访问只能走云函数
- 需求单状态机与四角色权限矩阵，`node:test` 覆盖（转移表、权限、过期判定、A/B 分桶、端云枚举一致性、详情页动作可见性）
- 服务端主干闭环：登录建档 → 发布 → 响应 → 选定 → 双方确认完成 / 取消，状态变更一律走事务，状态与审计日志同生共死
- 五个 Tab 与页面：需求广场（城市 + 品类筛选、分页）、发布页、需求单详情（三视角）、我的（档案 + 联系方式 + 我发布的 / 我响应的）
- 过期扫描定时任务（即时型 10 分钟、预约型 1 小时），让「需求单必然会终结」真的发生
- 埋点最小集与 A/B 分桶字段（M1 只埋不做实验）
- **选定可撤销**（D-35）：约不上可以退回待选定重新挑人，不必取消整单
- **达成共识后互相下发联系方式**（D-36）：只在「已确定」之后、只在双方之间；信用分口径改为以双方真实互评为主
- 收尾：删除临时的 `ping` 探针函数，初始配置写入迁至 `_shared/service/setupService.js`（由 `cron` 在云端手动触发，无客户端入口）

### 产品设计
- 定名「喊呗 / HeyBay」，AI 助手「小螺」；确立"在国外，喊一声就有人"的定位
- 完成 V2.0 完整 PRD（`memory-bank/design-document.md`）：把产品原子单位从"帖子"重构为"需求单"，城市社区定位为供给容器
- 确立不碰资金的商业化四阶段路线，以约定单 + 双向评价替代资金担保
- 确立 AI 战略定位"Agent 是供给的兜底层"与自主性阶梯 L0–L3
- 记录个人主体无法上线 UGC 类目的合规风险与 A/B 双 Demo 落地路径

### 工程
- 完成技术栈选型（`memory-bank/tech-stack.md`）：微信小程序原生 + 云开发，服务端边界为健壮性核心
- 初始化 git 仓库，建立 `.gitignore`（密钥与私有配置永不入库）

## [v1.0] - 历史基线
「同路人」海外旅行搭子小程序，原生微信小程序，11 个页面 + 自定义 tabBar，无后端。设计资产见 `docs/v1-assets/`。
