# 喊呗 HeyBay — 项目上下文与协作约定

本文件会在每次对话开始时自动加载。目的是让新会话在不重复讨论的前提下立即进入状态。

## 这是什么项目

海外旅行场景下的"异地找人"微信小程序。中国旅行者到了海外城市，把落地后的具体需求喊出来（找搭子、找付费地陪、借物易物、找住宿、打听本地信息、代购跑腿、翻译陪同、应急求助），当地常驻的华人/留学生顺路接下来。

- 中文名 **喊呗** ｜ 英文名 **HeyBay** ｜ AI 助手 **小螺** ｜ 仓库名 `heybay`
- Slogan：在国外，喊一声就有人
- 前身是 V1.0「同路人」搭子社区（代码见 `miniprogram/`，git tag `v1.0`）
- 首个样板城市：伦敦
- 北极星指标：每周成功撮合数；护栏指标：需求单首响率、本地响应者次月留存

## 先读这三份文档

1. `docs/design-document.md` — V2.0 完整 PRD（定位、需求单机制、信任与安全、AI 设计、指标、合规、商业化、Roadmap）
2. `docs/tech-stack.md` — 技术栈选型与理由，含"明确不用的技术"和技术风险
3. `docs/decision-log.md` — 25 条过程性决策，记录被否决的方案与否决理由

**在提出方案前先查 `decision-log.md`。** 如果你的建议与其中某条决策冲突，先说明冲突的是哪条、为什么现在情况不同，再提。不要重新论证已经定过的事。

## 项目现状

产品设计完成，V2.0 开发未启动。V1.0 是原生微信小程序（11 个页面 + 自定义 tabBar），无后端。

## 硬约束（不要提议绕过）

- **开发者是个人主体，无公司**，业余时间投入，资金投入近 0。任何方案都要先过"一个人做不做得出来、养不养得起"。
- **微信小程序个人主体无法上线 UGC 社区类目**（发帖/评论/私信是禁区）。V2.0 以**体验版**形态发布并真实运营。
- **平台不碰资金**：不代收不代付，付费需求线下结算，用约定单 + 双向评价做信誉担保。不要提议接入任何支付。
- **不采集身份证件影像**，不采集精确位置，不采集第三方联系人（GDPR）。
- **AI 不可代填**金额、见面时间、见面地点、联系方式四类字段。
- **不做交友属性**：品类白名单只有 8 类，性别偏好只提供"仅同性响应"，不提供"仅异性响应"。
- **不做 AI 全自动代人协商**（自主性阶梯的 L3 明确不做）。

## 技术约定

- 客户端：微信小程序**原生**（不用 Taro/uni-app，不做 TypeScript 全量迁移）
- 后端：微信云开发云函数，分层 `handler → service → dao`，业务逻辑不下沉到数据库查询
- **前端不可信**：所有状态变更只能走 `transitionRequest` 单一云函数入口，数据库权限设为仅云函数可写
- AI：所有能力经 `aiGateway` 单一出口（额度、缓存、成本记账、降级）；密钥只在云函数环境变量
- AI 失败必须能降级，永不阻断主流程
- 测试只覆盖三块纯逻辑：需求单状态机、AI 额度计算、分发匹配与频控
- 密钥永不入库：`.env`、`project.private.config.json` 已在 `.gitignore`

## 协作方式

- 需要判断时给**明确推荐 + 理由**，不要罗列选项让我选。
- 大型产出（文档、方案）前先澄清开放项，不要急着交付。
- 主动指出资质、平台规则、API 可行性上的硬约束和不可行项。早说坏消息，不要后期返工。
- 涉及微信平台规则、模型定价、云开发额度这类会变的事实，**先核实当期文档再回答**，不要凭记忆断言。PRD 附录 C 和技术栈第 15 节列了待核实清单。
- 输出注意错别字。
- 文档与代码统一用 喊呗 / HeyBay / 小螺，不要出现其他英文名变体。

## 版本管理

`main` 分支 + tag（`v1.0` = 同路人代码基线，`v2.0-design` = V2.0 设计完成）+ `CHANGELOG.md`。目录与仓库名不带版本号。
